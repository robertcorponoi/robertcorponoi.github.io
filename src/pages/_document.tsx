import { Html, Head, Main, NextScript } from "next/document";

/**
 * The `_document` page can be used to set `lang`, load fonts, and load
 * scripts.
 */
const Document = () => {
    return (
        <Html lang="en">
            <Head />
            <body>
                <Main />
                <NextScript />
            </body>
        </Html>
    );
};

export default Document;
